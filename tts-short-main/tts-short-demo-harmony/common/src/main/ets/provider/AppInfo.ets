export interface AppInfo {
  icon: string,
  name: string,
  color: string,
  productId: number,
  versionCode: number,
  versionName: string,
  debug: boolean,
  privacyUrl: string,
  agreementUrl: string,
}